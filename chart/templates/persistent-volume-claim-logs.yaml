{{- if .Values.persistence.logs.enabled }}
---
kind: "PersistentVolumeClaim"
apiVersion: {{ .Values.apiVersions.persistentVolumeClaim | quote }}
metadata:
  name: {{ .Values.persistence.logs.pvcName | quote }}
  namespace: {{ include "names.namespace" . | quote }}
  labels:
    {{- include "labels.commonLabels" . | nindent 4 }}
  {{- with .Values.persistence.logs.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
  {{- range .Values.persistence.logs.accessModes }}
    - {{ . | quote}}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.logs.size | quote }}
  storageClassName: {{ .Values.persistence.logs.storageClass | quote }}
...
{{- end }}
